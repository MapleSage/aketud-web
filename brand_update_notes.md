# AKETUD brand update notes

The cloned Astro repository builds **18 static routes** successfully. The shared site settings now point to the supplied A + AKETUD deep-blue and inverse PNG lockups. Both Base and ElevateBase layouts reference the A-mark browser favicon set, with updated PNG fallback sizes and a matching blue app-icon asset.

The homepage now opens with an AKETUD-specific “AI You Can Accept” hero. It keeps the existing insurance-operations chain and product sections underneath, but replaces the generic template first screen with a tonal-blue systems field, the supplied A mark, policy-state narrative, and three operating pillars: Underwriting, Claims, and Agency Management.

The brand override applies a warm-paper, graphite, and restrained moss palette to the existing shared theme. The completed route architecture and existing content remain intact.

## Preview checks

The local Astro preview is available at `https://4321-idevu687wbd5b6ft35lya-c950989a.sg2.manus.computer/`. The homepage renders the new hero and restored two-reversal logo successfully. The `/digital/` route preserves its service-page content and applies the shared AKETUD palette. The `/platform/` product route preserves the dark-editorial product layout while using the shared restored logo in its header and footer. All three pages resolved from the built Astro site without client-side console errors reported by the page preview.

## Blue platform direction

The supplied **A + AKETUD** lockup is now the active header and footer identity, with a deep platform-blue treatment on light surfaces and a white inverse treatment over the navigation and hero. The homepage hero uses the isolated supplied A as a low-contrast structural field and shifts the operations-chain surface from warm paper to cloud blue. The next route check will focus on ensuring that the inherited service and product pages carry the same blue tonal logic at desktop and mobile widths.

The Digital route now renders with a midnight-blue service hero, cloud-blue content field, dark-blue supplied lockup, and restrained blue-teal calls to action. The Platform route retains its deliberately separate dark editorial product treatment but now uses the same supplied logo in the shared header and footer, making the product screenshot material and public site feel more contiguous.

## Narrow viewport checks

At **375×812**, the homepage retains a clear A + AKETUD header lockup, readable primary action, and the large supplied-A field without obscuring the headline. The Digital page retains its dark-blue hero, complete title, readable body copy, and mobile navigation trigger. The homepage operating-pillar footer was adjusted so the accountability statement has its own full-width row below the three pillar labels rather than competing with them in a narrow column.

## Favicon treatment

The browser icon now uses the supplied A on the same deep platform-blue tile used by the operations interface. The 16px render keeps a clear white A silhouette rather than disappearing as a thin dark outline against arbitrary browser chrome. The public-site lockup remains transparent and unchanged; the tile is restricted to browser/app icon contexts.
